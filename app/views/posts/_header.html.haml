.headernav
  .headeruppercontent
    .headeruppercontent__title
      %i.fa.fa-plane
      Tripper
  .headerusericon
    %text
      = current_user&.name

  .headerundercontent
    .headerundercontent__btn
      - if user_signed_in?
        .headerundercontent__btn--logout
          = link_to "ログアウト", destroy_user_session_path, method: :delete
        .headerundercontent__btn--mypage
          = link_to "マイページ", "/users/#{current_user.id}"
      - else
        .headerundercontent__btn--logout
          = link_to "ログイン", new_user_registration_path, class: 'post'
        .headerundercontent__btn--logout
          = link_to "新規登録", new_user_registration_path, class: 'post'
      -# - if user_signed_in?
      -#   .headerundercontent__btn--useredit
      -#     = link_to edit_user_path(current_user) do
      -#       編集
      -# - else
      -#   .headerundercontent__btn--regist
      -#     = link_to new_user_registration_path do
      -#       新規登録
      -#   .headerundercontent__btn--login
      -#     -# = link_to ,new_user_registration_path,
      -#     ログイン

        
